fastapi==0.115.0
uvicorn==0.30.6
sqlalchemy==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
httpx==0.27.2
python-multipart==0.0.12
clickhouse-connect==0.7.19
alembic==1.13.3
pydantic==2.9.2
pydantic-settings==2.5.2
authlib==1.3.2
itsdangerous==2.2.0
psutil==5.9.8

# Core (most commonly used)
pymysql
oracledb
snowflake-connector-python
snowflake-sqlalchemy

# PostgreSQL-compatible (already have psycopg2-binary)
sqlalchemy-cockroachdb

# Analytics
duckdb
duckdb-engine
vertica-python
sqlalchemy-vertica-python

# Cloud
sqlalchemy-redshift
redshift-connector
sqlalchemy-bigquery
pyathena

# Other SQL
firebird-driver
sqlalchemy-firebird
hdbcli
sqlalchemy-hana

# Optional (install manually if needed):
# pyodbc              — SQL Server / Sybase (needs ODBC driver)
# ibm-db-sa           — DB2 (needs IBM client)
# teradatasqlalchemy  — Teradata
# trino               — Trino
# pyhive              — Hive / Spark
# databricks-sql-connector — Databricks
# elasticsearch-dbapi — Elasticsearch / OpenSearch
